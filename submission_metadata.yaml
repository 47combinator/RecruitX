# Redrob Hackathon — Submission Metadata
# Copy of submission_metadata_template.yaml with our team's details filled in.

team_name: "RecruitX"

primary_contact:
  name: "Pratyush"
  email: "pratyush@example.com"  # UPDATE with your actual email
  phone: "+91-XXXXXXXXXX"        # UPDATE with your actual phone

team_members:
  - name: "Pratyush"
    email: "pratyush@example.com"
    role: "ML Engineer / Lead"

github_repo: "https://github.com/YOUR_USERNAME/RecruitX"  # UPDATE

sandbox_link: "https://huggingface.co/spaces/YOUR_USERNAME/recruitx"  # UPDATE

reproduce_command: "python rank.py --candidates ./candidates.jsonl --out ./submission.csv"

compute:
  platform: "Windows PC"
  cpu_cores: 8
  ram_gb: 16
  python_version: "3.13"
  os: "Windows 11"
  uses_gpu_for_inference: false
  has_network_during_ranking: false
  pre_computation_required: true
  pre_computation_time_minutes: 20

ai_tools_used:
  - "Antigravity (Gemini)"

ai_usage_summary: |
  Used Antigravity (Gemini-based coding assistant) for architecture discussion,
  code structure, and implementation guidance. All engineering decisions,
  scoring weights, feature selection, and honeypot detection heuristics
  were designed and validated by the team. No candidate data was sent to
  any external LLM during development or ranking.

methodology_summary: |
  Hybrid retrieval + weighted ensemble ranking system. Dense retrieval
  via sentence-transformers (all-MiniLM-L6-v2) combined with BM25 sparse
  retrieval using Reciprocal Rank Fusion. 40+ features across skill match,
  career relevance, behavioral signals, education, and location are combined
  via a weighted ensemble scorer. Seven heuristic checks detect ~80 honeypot
  candidates with impossible profiles. Penalty multipliers handle consulting-only
  careers and keyword stuffers. Runtime ~60s for 100K candidates on CPU.

declarations:
  read_submission_spec: true
  code_is_original_work: true
  no_collusion: true
  honeypot_check_done: true
  reproduction_tested: true
